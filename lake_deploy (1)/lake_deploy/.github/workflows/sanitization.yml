name: sanitization-scan

on:
  push:
  pull_request:

jobs:
  sanitize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run sanitization scan
        run: |
          ./scripts/sanitize_scan.sh

